[ldap]
BindAccount = uid=*********,ou=users,dc=wyplay,dc=com
BindPassword = ************
serverip = ldap.mydomain.com
search_base = ou=USERS,dc=mydomaincontroller
user_filter = (objectclass=user) #note: compatibility to openldap is given. the dn funtions in spec2policy.py might need adjustment to suit your ldapserver

[matrix]
AdminUser = admin
AdminPassword = ************    #(not needed if the admin token is provided)
SharedSecret = ***************   #(optional)
Admintoken =  syt_****************+ #admin token is required, it can be generated curl                    
